"""Bridge entrypoints for external collaboration and task platforms.

DocMap's workspace graph is canonical. Integrations in this package project
that graph outward and persist only external mapping/projection metadata unless
a separate user-reviewed graph mutation accepts external input.
"""
